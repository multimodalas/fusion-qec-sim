# v3.8.0 — Structural Geometry Intervention

1. Add deterministic RPC (Redundant Parity Check) augmentation as an opt-in decoder extension.
2. Introduce `geom_v1` BP schedule with geometry-aware check-to-variable scaling.
3. Integrate RPC builder into BPAdapter via StructuralConfig gating.
4. All changes are additive; baseline decoding remains bit-identical when disabled.
5. No schema bumps, no channel changes, no heuristics in this release.
