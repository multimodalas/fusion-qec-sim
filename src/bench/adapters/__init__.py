"""
Decoder adapter implementations for the benchmarking framework.

Each adapter wraps an existing decoder entrypoint behind a uniform
:class:`DecoderAdapter` interface so the benchmark runner can treat
all decoders identically.
"""

__all__: list[str] = []
